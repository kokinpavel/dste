services:
  app:
    build:
      context: .
      dockerfile: docker/Dockerfile
    volumes:
      - .:/app
    working_dir: /app
    ports:
      - "8890:8890"
    command: jupyter notebook --port=8890 --no-browser --ip=0.0.0.0 --allow-root